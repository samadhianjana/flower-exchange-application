# Auditability Verification Notes

- Execution reports are persisted in write order through `InMemoryRepository::SaveReports`.
- Every execution report includes `transaction_time` generated by the matching engine.
- Batch summaries store totals for accepted and rejected rows.
- Realtime event stream mirrors persisted reports for observability checks.
- Replay hook for Phase 1: use persisted report sequence as deterministic replay source.
